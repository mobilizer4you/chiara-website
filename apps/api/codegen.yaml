overwrite: true
schema: './src/graphql/typedef.ts'
generates:
  generated/resolversTypes.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-resolvers
    config:
      useIndexSignature: true
